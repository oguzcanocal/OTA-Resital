@model RESITALMVC.MODEL.Entities.Room
@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPage.cshtml";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Hotel Düzenleme</h1>
                </div>
                <div class="col-sm-6">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Anasayfa</a></li>
                        <li class="breadcrumb-item active">Oda Güncelleme</li>
                    </ol>
                </div>
            </div>
        </div><!-- /.container-fluid -->
    </section>


    <!-- Main content -->
    <section class="content">

        @using (Html.BeginForm("Edit","Rooms2",FormMethod.Post, new { HotelID = Model.HotelID }))
        {
            <div class="row">


                <div class="col-md-12">
                    <div class="card card-primary">
                        <div class="card-header">
                            <h3 class="card-title">Oda Genel Bilgileri</h3>

                            <div class="card-tools">
                                <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                                    <i class="fas fa-minus"></i>
                                </button>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-12">
                                    <div class="form-group">
                                        <input type="hidden" name="HotelID" value="@Model.HotelID" />
                                        <label for="inputName">Oda Adı</label>
                                        @Html.ValidationMessageFor(Model => Model.RoomName, null, new { @class = "text-danger" })
                                        @Html.TextBoxFor(Model => Model.RoomName, new { @class = "form-control" })
                                    </div>
                                    <div class="form-group">
                                        <label for="inputName">Oda Açıklaması</label>
                                        @Html.ValidationMessageFor(Model => Model.RoomDescription, null, new { @class = "text-danger" })
                                        @Html.TextBoxFor(Model => Model.RoomDescription, new { @class = "form-control" })
                                    </div>
                                    <div class="form-group">
                                        <label for="inputDescription">Oda Müsaitlik</label>
                                        @Html.ValidationMessageFor(Model => Model.RoomAvailability, null, new { @class = "text-danger" })
                                        @Html.TextBoxFor(Model => Model.RoomAvailability, new { @class = "form-control" })
                                    </div>
                                </div>
                            </div>
                        </div>
                        <!-- /.card-body -->
                    </div>
                    <div class="col-md-12">
                        <a href=@Url.Action("Listing","Rooms2") class="btn btn-secondary">İptal</a>
                        <input type="submit" value="Kaydet" class="btn btn-success float-right">
                    </div>
                    <!-- /.card -->
                </div>




            </div>

        }


    </section>
    <!-- /.content -->
    <!-- /.content-wrapper -->
</div>

